# DataMarket — Environment Variables
# Copy this file to .env and fill in your values for local development.
# In Databricks Apps, DATABRICKS_HOST and DATABRICKS_TOKEN are injected automatically.

# ── Databricks (auto-injected in Databricks Apps) ────────────────────────────
DATABRICKS_HOST=https://your-workspace.azuredatabricks.net
DATABRICKS_TOKEN=your-personal-access-token
DATABRICKS_USER=you@your-domain.com

# ── Lakebase (Postgres) ───────────────────────────────────────────────────────
# Get the host from: databricks database get-database-instance YOUR_INSTANCE
LAKEBASE_HOST=instance-xxxxxxxx.database.azuredatabricks.net
LAKEBASE_INSTANCE=your-lakebase-instance-name
LAKEBASE_DB=your_database_name
LAKEBASE_SCHEMA=datamarket
# Set to 'false' only for self-signed certs in dev/POC environments; leave unset (default: true) for production
# LAKEBASE_SSL_REJECT_UNAUTHORIZED=false

# ── Frontend (Vite) ───────────────────────────────────────────────────────────
# Optional: link to your published AI/BI dashboard
VITE_DASHBOARD_URL=https://your-workspace.azuredatabricks.net/sql/dashboardsv3/your-dashboard-id
